const RECEIPT_HEADER = 'item              price        qty';

const LEN_RECEIPT = 33;
const LEN_RECEIPT_PRICE = 22;
const LEN_CITY = 2;

const SPILT_IN_INPUT = ', ';
const SPILT_IN_PURCHASED = ' at ';

const TEXT_TOTAL = 'total';
const TEXT_SUBTOTAL = 'subtotal';
const TEXT_OUTPUT = 'Output';
const TEXT_TAX = 'tax';

const CATEGORY＿FOOD = ['potato chips'];
const CATEGORY＿CLOTHING = ['shirt'];


module.exports = {
    RECEIPT_HEADER,
    LEN_RECEIPT,
    LEN_RECEIPT_PRICE,
    LEN_CITY,
    SPILT_IN_INPUT,
    SPILT_IN_PURCHASED,
    TEXT_TOTAL,
    TEXT_SUBTOTAL,
    TEXT_OUTPUT,
    TEXT_TAX,
    CATEGORY＿FOOD,
    CATEGORY＿CLOTHING,
}
